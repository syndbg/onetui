# onetui-dynamodb

Native AWS DynamoDB and Streams clients. The connector keeps attribute tags and decimal strings intact, bounds requests and pages, and never sends write operations.

See [usage](../../docs/dynamodb.md) and `onetui schema --datasource dynamodb`.
